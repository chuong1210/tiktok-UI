const images = {
    logo: require('~/assests/image/logo.svg').default,
    computer: require('~/assests/image/computer.svg').default,
    send: require('./send.svg').default,
    noImage: require('./no-images.png'),
};

export default images;
